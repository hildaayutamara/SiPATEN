import React, { useState } from "react";
//import { Form } from "react-bootstrap";
//import { v4 as uuidv4 } from "uuid";
import { Button, Modal } from "react-bootstrap";
import "./App.css";
//import axios from "axios";

const DetailKompetensi = () => {
    /*const [Kompetensi, setKompetensi] = useState({
        id: uuidv4(),
        nama: "",
        nip: "",
        jenis: "",
        kompetensi: "",
        tanggal: "",
        jp: "",
        penyelenggara: "",
        sertifikat: "",
      });
    
      const onInputChange = (e) => {
        setKompetensi({ ...Kompetensi, [e.target.name]: e.target.value });
      };
    
      const { id, nama, nip, jenis, kompetensi, tanggal, jp, penyelenggara, sertifikat } = Kompetensi;
      const handleSubmit = async () => {
        await axios.post(`http://localhost:3200/data_kompetensi`, Kompetensi);
        alert("Sukses!");
      };
      console.log("data", Kompetensi);*/

  return (
    <>
       <Modal.Body>
      </Modal.Body>
      <Modal.Footer>
      </Modal.Footer>
    </>
  )
}

export default DetailKompetensi
